# === Consumer (ingesta) — conn-edgar ===
export CONSUMER="conn-edgar-test3"
export CONSUMER_HOST="${CONSUMER}.${DS_DOMAIN}"
export CONSUMER_BASE="https://${CONSUMER_HOST}"
export CONSUMER_PROTOCOL="http://${CONSUMER}:19194/protocol"
