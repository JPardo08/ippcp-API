# === Consumer (ingesta) — conn-company ===
export CONSUMER="conn-company-ippcp"
export CONSUMER_HOST="${CONSUMER}.${DS_DOMAIN}"
export CONSUMER_BASE="https://${CONSUMER_HOST}"
export CONSUMER_PROTOCOL="https://${CONSUMER_HOST}/protocol"
